import { SonDirective } from './son.directive';

describe('SonDirective', () => {
  it('should create an instance', () => {
    const directive = new SonDirective();
    expect(directive).toBeTruthy();
  });
});
